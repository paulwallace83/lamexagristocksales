# Deferred Findings — Batch B011

| # | Source | Severity | File | Finding | Reason Deferred | Target Batch | Status |
|---|--------|----------|------|---------|----------------|-------------|--------|
| 1 | integration | Important | [app/api/enquiries/route.ts:171-188](../../app/api/enquiries/route.ts#L171-L188) | Two parallel 429 paths in the same route return inconsistent JSON shapes. The in-memory limiter (now `checkEnquiryRateLimit` from `lib/enquiry-rate-limit.ts`) returns `{ error, retryAfter }` plus `Retry-After` header; the DB doc-request limiter still returns `{ error }` only. The client at `EnquiryForm.tsx` keys its friendly countdown off `typeof data.retryAfter === "number"`, so users who trip the DB limiter see the generic message while users who trip the in-memory limiter see the countdown. | Fix requires a new `getOldestRequestTimestamp(email)` function in `lib/document-requests.ts` and threading it through the route. The retro explicitly listed this as a future-batch item alongside the related Near-Term roadmap item "Enquiry tracking — persist all enquiries to DB", which will likely consolidate or replace the in-memory limiter entirely. Out of scope for B011 polish. | TBD — pair with "Enquiry tracking — persist all enquiries to DB" Near-Term roadmap item | OPEN |
| 2 | correctness | Minor | [app/api/enquiries/route.ts:35-44](../../app/api/enquiries/route.ts#L35-L44) (now in `lib/enquiry-rate-limit.ts`) | When `timestamps.length >= maxPerHour`, the function returns blocked but never writes the freshly-filtered `timestamps` array back into `enquiryTimestamps`. The unfiltered (potentially-stale) array stays in the map until either (a) the user successfully submits and the push branch persists a new filtered list, or (b) the 10-minute global cleanup tick runs. Subsequent blocked requests re-do the same `.filter()` work on the unpruned list. | Pre-existing pattern, not introduced by B011. Functionally correct (the local `timestamps` is what `Math.min` runs on); only a small inefficiency on a hot path that's already bounded to 5 entries. Same Near-Term batch will likely replace this code wholesale. | TBD — pair with finding #1 | OPEN |
